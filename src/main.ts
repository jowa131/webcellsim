// Entry point - wires up the canvas UI and worker
// Populated fully in Step 4 (canvas-ui skill)

const app = document.getElementById('app')!
app.innerHTML = '<p style="color:#888;padding:20px;">Initializing simulation engine...</p>'

export {}
